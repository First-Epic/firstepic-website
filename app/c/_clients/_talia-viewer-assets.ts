// Auto-generated from Talia ui_media_viewer.media_viewer_assets(). The universal
// TaliaViewer lightbox (event-delegated on data-tvw-src). Back-to-top IIFE removed -
// the shared <BackToTop/> client component owns that button.
export const TVW_HEAD = "<style>.tvw{position:fixed;inset:0;z-index:2147483000;background:rgba(3,4,6,.97);display:none;flex-direction:column;}\n.tvw.open{display:flex;}\n.tvw-bar{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;padding:14px 20px;border-bottom:1px solid rgba(255,255,255,.12);}\n.tvw-cap{color:#e8eaed;font-size:.98rem;line-height:1.42;max-width:82%;font-family:Inter,ui-sans-serif,system-ui,sans-serif;}\n.tvw-cap b{color:#c7cbff;font-weight:700;margin-right:8px;}\n.tvw-cap .sub{display:block;margin-top:6px;font-size:.82rem;color:#9aa2ac;}\n.tvw-tools{display:flex;align-items:center;gap:8px;flex:none;}\n/* the zoom-button group is a HORIZONTAL row (Davis 2026-08-21) - the wrapping span defaulted to\n   inline, so the display:grid buttons inside were stacking vertically. */\n#tvwZoom{display:inline-flex;align-items:center;gap:8px;}\n.tvw-btn{cursor:pointer;color:#cfd2d8;border:1px solid rgba(255,255,255,.16);background:rgba(255,255,255,.04);border-radius:8px;width:34px;height:34px;font-size:1rem;line-height:1;display:grid;place-items:center;}\n.tvw-btn:hover{color:#fff;border-color:rgba(129,140,248,.6);}\n.tvw.fs{background:rgba(0,0,0,.985);}\n.tvw.fs #tvwFs{color:#fff;border-color:rgba(129,140,248,.7);background:rgba(129,140,248,.16);}\n.tvw-stage{flex:1;overflow:auto;display:flex;align-items:center;justify-content:center;padding:18px;-webkit-overflow-scrolling:touch;\n  scrollbar-width:thin;scrollbar-color:rgba(129,140,248,.5) transparent;}\n/* stylized scrollbars in the lightbox (Davis 2026-08-21) - replace the default white/gray */\n.tvw-stage::-webkit-scrollbar{width:11px;height:11px;}\n.tvw-stage::-webkit-scrollbar-track{background:transparent;}\n.tvw-stage::-webkit-scrollbar-thumb{background:rgba(129,140,248,.42);border-radius:8px;border:2px solid rgba(3,4,6,.6);}\n.tvw-stage::-webkit-scrollbar-thumb:hover{background:rgba(129,140,248,.66);}\n.tvw-stage::-webkit-scrollbar-corner{background:transparent;}\n.tvw-stage.doc{align-items:stretch;padding:0;}\n/* flex:none is CRITICAL (Davis 2026-08-21): .tvw-stage is a flex box, so the <img> is a flex item with\n   the default flex-shrink:1 - which SHRINKS the zoomed image back to fit the stage the instant its set\n   width exceeds the window, capping zoom at window width. flex:none lets the explicit width stand so it\n   overflows and the stage (overflow:auto) scrolls/pans. */\n.tvw-stage img{display:inline-block;flex:none;user-select:none;-webkit-user-drag:none;cursor:grab;touch-action:none;border-radius:10px;background:#000;}\n.tvw-stage video{max-width:100%;max-height:100%;border-radius:10px;background:#000;border:1px solid rgba(255,255,255,.12);}\n.tvw-doc{width:min(920px,96%);height:100%;margin:0 auto;border:0;background:#fff;}\n.tvw-hint{font-size:.66rem;color:#6b7280;letter-spacing:.03em;padding:6px 20px 10px;text-align:center;font-family:Inter,ui-sans-serif,system-ui,sans-serif;}\n/* deck carousel controls (a referenced-work deck opens all its pages) */\n.tvw-decknav{position:fixed;left:50%;bottom:64px;transform:translateX(-50%);z-index:5;display:flex;\n  align-items:center;gap:14px;background:rgba(12,13,16,.86);border:1px solid rgba(255,255,255,.14);\n  border-radius:999px;padding:6px 10px;backdrop-filter:blur(6px);font-family:Inter,ui-sans-serif,system-ui,sans-serif;}\n.tvw-deckbtn{cursor:pointer;color:#cfd2d8;border:1px solid rgba(255,255,255,.18);background:rgba(255,255,255,.05);\n  border-radius:8px;width:34px;height:34px;font-size:1.2rem;line-height:1;display:grid;place-items:center;}\n.tvw-deckbtn:hover{color:#fff;border-color:rgba(129,140,248,.6);}\n.tvw-deckcount{color:#c7cbff;font-size:.82rem;font-weight:600;min-width:52px;text-align:center;font-variant-numeric:tabular-nums;}\n/* back-to-top */\n.tv-top{position:fixed;right:20px;bottom:20px;z-index:1200;width:44px;height:44px;border-radius:12px;cursor:pointer;\n  display:grid;place-items:center;font-size:1.15rem;color:#c7cbff;border:1px solid rgba(129,140,248,.45);\n  background:rgba(20,20,24,.86);backdrop-filter:blur(8px);opacity:0;transform:translateY(8px);\n  transition:opacity .2s,transform .2s,background .15s;pointer-events:none;box-shadow:0 6px 20px rgba(0,0,0,.35);}\n.tv-top.show{opacity:1;transform:translateY(0);pointer-events:auto;}\n.tv-top:hover{background:rgba(30,30,38,.95);color:#fff;}\n/* while a cockpit/viewer overlay is up, freeze the page behind it and hide the back-to-top */\nhtml.tvw-lock,body.tvw-lock{overflow:hidden !important;}\n.tvw-lock .tv-top{display:none !important;}</style>";
export const TVW_JS = "(function(){\n  if(window.TaliaLock) return;\n  var n=0, de=document.documentElement;\n  function set(on){ if(on){ de.classList.add(\"tvw-lock\"); if(document.body) document.body.classList.add(\"tvw-lock\"); }\n    else { de.classList.remove(\"tvw-lock\"); if(document.body) document.body.classList.remove(\"tvw-lock\"); } }\n  window.TaliaLock={ push:function(){ n++; set(true); }, pop:function(){ n=Math.max(0,n-1); if(n===0) set(false); } };\n})();\n(function(){\n  if(window.TaliaViewer) return;\n  var el=null, deckKeys=null;\n  function build(){\n    el=document.createElement(\"div\"); el.className=\"tvw\"; el.setAttribute(\"aria-hidden\",\"true\");\n    el.innerHTML='<div class=\"tvw-bar\"><div class=\"tvw-cap\" id=\"tvwCap\"></div>'\n      +'<div class=\"tvw-tools\">'\n      +'<span id=\"tvwZoom\">'\n      +'<button class=\"tvw-btn\" id=\"tvwOut\" title=\"Zoom out\" type=\"button\">&#8722;</button>'\n      +'<button class=\"tvw-btn\" id=\"tvwFit\" title=\"Fit\" type=\"button\">&#9633;</button>'\n      +'<button class=\"tvw-btn\" id=\"tvwIn\" title=\"Zoom in\" type=\"button\">&#43;</button></span>'\n      +'<a class=\"tvw-btn\" id=\"tvwTab\" title=\"Open in new tab\" target=\"_blank\" rel=\"noopener\" style=\"display:none;text-decoration:none\">&#8599;</a>'\n      +'<button class=\"tvw-btn\" id=\"tvwFs\" title=\"Full screen\" type=\"button\">'\n      +'<svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" '\n      +'stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M8 3H5a2 2 0 0 0-2 2v3M16 3h3a2 2 0 0 1 2 2v3'\n      +'M8 21H5a2 2 0 0 1-2-2v-3M16 21h3a2 2 0 0 1 2-2v-3\"/></svg></button>'\n      +'<button class=\"tvw-btn\" id=\"tvwClose\" title=\"Close\" type=\"button\">&#10005;</button></div></div>'\n      +'<div class=\"tvw-stage\" id=\"tvwStage\"></div>'\n      +'<div class=\"tvw-hint\" id=\"tvwHint\"></div>';\n    document.body.appendChild(el);\n    el.addEventListener(\"click\", function(ev){ if(ev.target===el) close(); });\n    document.getElementById(\"tvwClose\").addEventListener(\"click\", close);\n    document.getElementById(\"tvwFs\").addEventListener(\"click\", function(ev){ ev.stopPropagation(); toggleFS(); });\n    document.addEventListener(\"keydown\", function(ev){\n      if(!el.classList.contains(\"open\") || ev.key!==\"Escape\") return;\n      // Esc backs out of full-screen first, then closes the viewer.\n      if(el.classList.contains(\"fs\")){ ev.preventDefault();\n        if(document.fullscreenElement){ try{ document.exitFullscreen(); }catch(e){} } else applyFS(false); }\n      else close();\n    });\n    document.addEventListener(\"fullscreenchange\", function(){\n      if(el && el.classList.contains(\"open\")) applyFS(document.fullscreenElement===el); });\n  }\n  // Full-screen toggle (native Fullscreen API; CSS-maximize fallback). When zoomed, the zoom LEVEL is\n  // preserved across the toggle (remeasureKeep); when unzoomed it re-fits to the new size. Davis 2026-08-28.\n  function applyFS(on){ if(!el) return; el.classList.toggle(\"fs\", on);\n    if(zoom){ if(zoom.zoomed()) zoom.remeasureKeep(); else zoom.refit(); } }\n  function toggleFS(){ if(!el) return;\n    var isFS = el.classList.contains(\"fs\") || document.fullscreenElement===el;\n    if(isFS){ if(document.fullscreenElement){ try{ document.exitFullscreen(); }catch(e){ applyFS(false); } } else applyFS(false); }\n    else if(el.requestFullscreen){ el.requestFullscreen().catch(function(){ applyFS(true); }); } else applyFS(true); }\n  var zoom=null;\n  function mkZoom(im, pane){\n    var z=1, fitW=0, aspect=1, ZMAX=16, STEP=1.35;   // zoom well past screen width (Davis 2026-08-21)\n    function _fs(){ return !!(el && el.classList.contains(\"fs\")); }\n    function measure(){\n      var mh=window.innerHeight*(_fs()?0.94:0.80), mw=window.innerWidth*(_fs()?0.98:0.94);\n      var nw=im.naturalWidth||im.width||1, nh=im.naturalHeight||im.height||1;\n      aspect=nw/nh; fitW=Math.min(mw, mh*aspect); if(!fitW||!isFinite(fitW)) fitW=nw;\n    }\n    function render(){\n      im.style.maxWidth=\"none\"; im.style.maxHeight=\"none\"; im.style.height=\"auto\";\n      var w=fitW*z; im.style.width=w+\"px\"; im.style.cursor=(z>1.001)?\"grab\":\"default\";\n      // when the image overflows the pane on an axis, switch that axis from centered to start so the\n      // hidden edge is reachable by scroll/pan (centered flex clips the overflow unreachably).\n      if(pane){\n        pane.style.justifyContent = (w > pane.clientWidth+1) ? \"flex-start\" : \"center\";\n        pane.style.alignItems     = ((w/aspect) > pane.clientHeight+1) ? \"flex-start\" : \"center\";\n      }\n    }\n    function refit(){ measure(); z=1; render(); if(pane){ pane.scrollTop=0; pane.scrollLeft=0; } }\n    // Zoom keeping the point (cx,cy) - coords WITHIN the pane's viewport - fixed. Default = the CENTER\n    // of the visible frame, so the +/- buttons zoom from center (not a corner). The anchored fraction is\n    // read from the IMAGE's own rect, so it is correct whether the image is flex-centered or scrolled.\n    // (Ported from _ZLB_JS anchorZoom, Davis 2026-08-28.)\n    function anchorZoom(factor, cx, cy){\n      var pw=(pane&&pane.clientWidth)||0, ph=(pane&&pane.clientHeight)||0;\n      if(cx==null) cx=pw/2; if(cy==null) cy=ph/2;\n      var pr=pane?pane.getBoundingClientRect():{left:0,top:0}, ir=im.getBoundingClientRect();\n      var iw=ir.width||1, ih=ir.height||1;\n      var fx=((pr.left+cx)-ir.left)/iw, fy=((pr.top+cy)-ir.top)/ih;\n      fx=Math.max(0,Math.min(1,fx)); fy=Math.max(0,Math.min(1,fy));\n      z=Math.max(1,Math.min(ZMAX, z*factor)); render();\n      var nw=im.offsetWidth||1, nh=im.offsetHeight||1;\n      if(pane){ pane.scrollLeft=fx*nw-cx; pane.scrollTop=fy*nh-cy; }\n    }\n    // Re-fit for a new (full-screen) pane size but KEEP the current on-screen size + center - so the zoom\n    // level PERSISTS across the fullscreen toggle when zoomed; only re-fit when un-zoomed. Davis 2026-08-28.\n    function remeasureKeep(){\n      var pw=(pane&&pane.clientWidth)||0, ph=(pane&&pane.clientHeight)||0;\n      var pr=pane?pane.getBoundingClientRect():{left:0,top:0}, ir=im.getBoundingClientRect();\n      var iw=ir.width||1, ih=ir.height||1;\n      var fx=((pr.left+pw/2)-ir.left)/iw, fy=((pr.top+ph/2)-ir.top)/ih, disp=fitW*z;\n      measure(); z=Math.max(1,Math.min(ZMAX, disp/fitW)); render();\n      var nw=im.offsetWidth||1, nh=im.offsetHeight||1;\n      if(pane){ pane.scrollLeft=fx*nw-pw/2; pane.scrollTop=fy*nh-ph/2; }\n    }\n    return {refit:refit, reset:refit, zoomed:function(){ return z>1.001; },\n      anchorZoom:anchorZoom, remeasureKeep:remeasureKeep,\n      zin:function(){ anchorZoom(STEP); }, zout:function(){ anchorZoom(1/STEP); },\n      pinch:function(m,cx,cy){ anchorZoom(m,cx,cy); },\n      wheel:function(d,cx,cy){ anchorZoom(d<0?STEP:1/STEP, cx, cy); }};\n  }\n  function clearStage(){ var s=document.getElementById(\"tvwStage\"); s.innerHTML=\"\"; s.className=\"tvw-stage\"; zoom=null;\n    if(deckKeys){ document.removeEventListener(\"keydown\", deckKeys); deckKeys=null; } }\n  function open(opts){\n    opts=opts||{}; if(!el) build();\n    var cap=document.getElementById(\"tvwCap\"), stage=document.getElementById(\"tvwStage\"),\n        tools=document.getElementById(\"tvwTools\"), hint=document.getElementById(\"tvwHint\");\n    cap.innerHTML=\"\"; if(opts.label){ var b=document.createElement(\"b\"); b.textContent=opts.label; cap.appendChild(b); }\n    if(opts.sub){ var d=document.createElement(\"span\"); d.className=\"sub\"; d.textContent=opts.sub; cap.appendChild(d); }\n    clearStage();\n    var showZoom=false, h=\"\";\n    if(opts.kind===\"video\"){\n      var v=document.createElement(\"video\"); v.controls=true; v.playsInline=true; v.preload=\"metadata\"; v.src=opts.src; // NEVER autoplay\n      stage.appendChild(v);\n    } else if(opts.kind===\"doc\"){\n      stage.className=\"tvw-stage doc\";\n      var f=document.createElement(\"iframe\"); f.className=\"tvw-doc\"; f.src=opts.src; stage.appendChild(f);\n      h=\"Scroll to read the rest of the document.\";\n    } else {\n      // IMAGE, or DECK = a paged carousel that can MIX still images and VIDEO (Davis 2026-08-21: a\n      // campaign deck may include a motion piece among the stills). `items`: [{t:'image'|'video',src}];\n      // a legacy string entry (or opts.src) is an image. An image page gets the zoom/pan engine; a video\n      // page gets a native <video> (no autoplay) and hides the zoom buttons.\n      var raw=(opts.kind===\"deck\" && opts.images && opts.images.length) ? opts.images : [opts.src];\n      var items=[]; for(var _i=0;_i<raw.length;_i++){ var _e=raw[_i];\n        if(_e && typeof _e===\"object\"){ items.push({t:(_e.type===\"video\"?\"video\":\"image\"), src:(_e.src||_e.url||\"\")}); }\n        else { items.push({t:\"image\", src:_e}); } }\n      var page=0, counter=null, curMedia=null, pinching=false, pd=0;\n      showZoom=true; h=\"Scroll wheel or the + / - buttons to zoom &nbsp;&middot;&nbsp; drag to pan &nbsp;&middot;&nbsp; pinch on touch\";\n      function _setZoomVis(on){ var zb=document.getElementById(\"tvwZoom\"); if(zb) zb.style.display=on?\"\":\"none\"; }\n      function mount(){\n        zoom=null; if(curMedia){ try{ curMedia.remove(); }catch(e){} curMedia=null; }\n        var it=items[page]||{t:\"image\",src:\"\"};\n        if(it.t===\"video\"){\n          _setZoomVis(false);\n          var v=document.createElement(\"video\"); v.controls=true; v.playsInline=true; v.preload=\"metadata\"; v.src=it.src; // NEVER autoplay\n          stage.appendChild(v); curMedia=v;\n        } else {\n          _setZoomVis(true);\n          var im=document.createElement(\"img\"); im.alt=opts.label||\"\"; stage.appendChild(im); curMedia=im;\n          var cur=im;\n          var setup=function(){ if(curMedia!==cur) return; zoom=mkZoom(im, stage); zoom.refit(); };\n          im.src=it.src;\n          if(im.complete && im.naturalWidth) setup(); else im.addEventListener(\"load\", setup, {once:true});\n          // grab-to-pan via scroll offsets (lifted from the contact-sheet lightbox), bound to THIS img\n          var down=false,sx=0,sy=0,sl=0,st=0;\n          im.addEventListener(\"pointerdown\", function(ev){ if(pinching||!zoom||!zoom.zoomed()) return; down=true;\n            sx=ev.clientX; sy=ev.clientY; sl=stage.scrollLeft; st=stage.scrollTop; im.style.cursor=\"grabbing\";\n            try{ im.setPointerCapture(ev.pointerId); }catch(e){} ev.preventDefault(); });\n          im.addEventListener(\"pointermove\", function(ev){ if(!down||pinching) return;\n            stage.scrollLeft=sl-(ev.clientX-sx); stage.scrollTop=st-(ev.clientY-sy); });\n          function _up(){ down=false; im.style.cursor=(zoom&&zoom.zoomed())?\"grab\":\"default\"; }\n          im.addEventListener(\"pointerup\", _up); im.addEventListener(\"pointercancel\", _up);\n          im.addEventListener(\"dragstart\", function(ev){ ev.preventDefault(); });\n        }\n        if(counter) counter.textContent=(page+1)+\" / \"+items.length;\n      }\n      if(items.length>1){\n        var nav=document.createElement(\"div\"); nav.className=\"tvw-decknav\";\n        var prev=document.createElement(\"button\"); prev.className=\"tvw-deckbtn\"; prev.type=\"button\"; prev.innerHTML=\"&#8249;\";\n        var next=document.createElement(\"button\"); next.className=\"tvw-deckbtn\"; next.type=\"button\"; next.innerHTML=\"&#8250;\";\n        counter=document.createElement(\"span\"); counter.className=\"tvw-deckcount\";\n        prev.onclick=function(){ page=(page-1+items.length)%items.length; mount(); };\n        next.onclick=function(){ page=(page+1)%items.length; mount(); };\n        nav.appendChild(prev); nav.appendChild(counter); nav.appendChild(next); stage.appendChild(nav);\n        h+=\" &nbsp;&middot;&nbsp; &#8249; / &#8250; or arrow keys to page\";\n        deckKeys=function(ev){ if(ev.key===\"ArrowLeft\"){ prev.onclick(); } else if(ev.key===\"ArrowRight\"){ next.onclick(); } };\n        document.addEventListener(\"keydown\", deckKeys);\n      }\n      // WHEEL + PINCH zoom bound to the STAGE (persistent across pages; no-op on a video page where zoom=null).\n      // Both anchor at the pointer: the WHEEL at the cursor, the PINCH at the two-finger midpoint (Davis 2026-08-28).\n      stage.addEventListener(\"wheel\", function(ev){ if(!zoom) return; ev.preventDefault();\n        var r=stage.getBoundingClientRect(); zoom.wheel(ev.deltaY, ev.clientX-r.left, ev.clientY-r.top); }, {passive:false});\n      function _dist(ts){ var dx=ts[0].clientX-ts[1].clientX, dy=ts[0].clientY-ts[1].clientY; return Math.sqrt(dx*dx+dy*dy); }\n      stage.addEventListener(\"touchstart\", function(e){ if(e.touches.length===2){ pinching=true; pd=_dist(e.touches); } }, {passive:false});\n      stage.addEventListener(\"touchmove\", function(e){ if(e.touches.length===2 && zoom){ e.preventDefault();\n        var d=_dist(e.touches); if(pd>0&&d>0){ var r=stage.getBoundingClientRect();\n          var mx=(e.touches[0].clientX+e.touches[1].clientX)/2-r.left, my=(e.touches[0].clientY+e.touches[1].clientY)/2-r.top;\n          zoom.pinch(d/pd, mx, my); } pd=d; } }, {passive:false});\n      stage.addEventListener(\"touchend\", function(e){ if(e.touches.length<2){ pd=0; if(e.touches.length===0) pinching=false; } });\n      mount();\n    }\n    // Hide only the ZOOM buttons for non-images; the CLOSE (x) button always stays (fix: it used to\n    // vanish with the whole toolbar on video/doc, leaving no way out - Davis 2026-08-21).\n    document.getElementById(\"tvwZoom\").style.display=showZoom?\"\":\"none\";\n    // \"Open in new tab\" (Davis 2026-08-21): for a doc opened in the viewer (JD/ITP), a companion link\n    // to open the SAME rasterized page full-screen in a new tab - still our viewer, never chrome://pdf.\n    var _tab=document.getElementById(\"tvwTab\");\n    if(_tab){ if(opts.tab){ _tab.href=opts.tab; _tab.style.display=\"\"; } else { _tab.style.display=\"none\"; _tab.removeAttribute(\"href\"); } }\n    if(showZoom){ document.getElementById(\"tvwIn\").onclick=function(){ zoom&&zoom.zin(); };\n      document.getElementById(\"tvwOut\").onclick=function(){ zoom&&zoom.zout(); };\n      document.getElementById(\"tvwFit\").onclick=function(){ zoom&&zoom.reset(); }; }\n    hint.innerHTML=h;\n    el.classList.add(\"open\"); el.setAttribute(\"aria-hidden\",\"false\");\n    window.TaliaLock && window.TaliaLock.push();   // lock background scroll while open\n    // Re-fit AFTER the overlay is visible (Davis 2026-08-21): the first refit ran while the stage was\n    // still display:none, so stage.clientWidth was 0 and the fit image was pinned flex-start (left) until\n    // a manual zoom re-rendered it. Re-measuring now that the stage has real width centers it immediately.\n    if(zoom){ requestAnimationFrame(function(){ if(zoom&&zoom.reset) zoom.reset(); }); }\n  }\n  function close(){ if(!el) return;\n    try{ if(document.fullscreenElement) document.exitFullscreen(); }catch(e){}\n    el.classList.remove(\"fs\");\n    clearStage(); el.classList.remove(\"open\"); el.setAttribute(\"aria-hidden\",\"true\");\n    window.TaliaLock && window.TaliaLock.pop(); }\n  window.TaliaViewer={open:open, close:close};\n})();\n(function(){\n  // Universal DECLARATIVE trigger: any element with data-tvw-src opens the viewer, so a tile\n  // anywhere on any Talia page is a one-attribute call with no per-page script + no huge data:\n  // URI stuffed into an inline onclick. kind/label/sub are optional companion attributes.\n  document.addEventListener(\"click\", function(e){\n    var t = e.target && e.target.closest ? e.target.closest(\"[data-tvw-src]\") : null;\n    if(!t || !window.TaliaViewer) return;\n    e.preventDefault();\n    window.TaliaViewer.open({\n      kind: t.getAttribute(\"data-tvw-kind\") || \"image\",\n      src: t.getAttribute(\"data-tvw-src\") || \"\",\n      label: t.getAttribute(\"data-tvw-label\") || \"\",\n      sub: t.getAttribute(\"data-tvw-sub\") || \"\",\n      tab: t.getAttribute(\"data-tvw-tab\") || \"\"\n    });\n  }, false);\n})();";
